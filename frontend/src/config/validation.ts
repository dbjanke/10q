/**
 * Frontend validation constants
 * 
 * Note: Keep these in sync with backend/src/config/validation.ts
 * Frontend limits should be <= backend limits
 */

export const MAX_TITLE_LENGTH = 50;
export const MAX_RESPONSE_LENGTH = 2000;
export const MAX_GROUP_NAME_LENGTH = 64;
