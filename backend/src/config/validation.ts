/**
 * Backend validation constants
 * 
 * Note: Keep these in sync with frontend/src/config/validation.ts
 * Backend limits should be >= frontend limits for security
 */

export const MAX_TITLE_LENGTH = 50;
export const MAX_RESPONSE_LENGTH = 2000;
export const MAX_GROUP_NAME_LENGTH = 64;
